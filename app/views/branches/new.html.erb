<%= form_for(@branch) do |f| %>
  <% if @branch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@branch.errors.count, "error") %> prohibited this co applicants from being saved:</h2>

      <ul>
      <% @branch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field_name">
    <%= f.label :Branch_name %>
    <%= f.text_field :name %>
  </div>

  <div class="field_name">
    <%= f.label :Branch_Code %>
    <%= f.text_field :branch_code %>
  </div>

  <div class="field_name">
    <%= f.label :Contact_person %>
    <%= f.text_field :contact_person %>
  </div>

   <%= f.submit %>
<% end %>
