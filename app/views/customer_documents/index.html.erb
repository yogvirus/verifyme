<h1>Listing customer_documents</h1>

<table>
  <tr>
    <th>Customer</th>
    <th>Photo</th>
    <th>Photo name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @customer_documents.each do |customer_document| %>
  <tr>
    <td><%= customer_document.customer_id %></td>
    <td><%= customer_document.photo %></td>
    <td><%= customer_document.photo_name %></td>
    <td><%= link_to 'Show', customer_document %></td>
    <td><%= link_to 'Edit', edit_customer_document_path(customer_document) %></td>
    <td><%= link_to 'Destroy', customer_document, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Customer document', new_customer_document_path %>
