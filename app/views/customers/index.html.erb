<style>
input[type=text] {
	font-size:9px;
}
input[type=submit] {
	font-size:14px;
  margin-top:13px;
}
.input-mysize { width: 150px }
</style>
<!--
<%= form_tag customer_search_path, :method => 'get' do %>
<div class="topic_form" style="float:right;display: block; ">
<table>
 <tr>
  <td>
   Search Customer :<br>
   <%= text_field_tag :search, params[:search], :class=>"span6 input-xxlarge input-large" %>
  </td>
  <td>
    <%= submit_tag 'Search', :class=>"btn btn-primary" %>
  </td>
 </tr>
</table>
</div>
<% end %>
-->

<div id="residential">
<b> Filter by : <%= link_to "residential" , customers_cust_residential_path %> |
                <%= link_to "business" , customers_cust_business_path %> </b><br>
 <b> Filter by :
                <%= link_to "Pending" , customers_cust_pending_path %> |
                <%= link_to "in progress" , customers_cust_in_progress_path %> |
                <%= link_to "completed" , customers_cust_completed_path %>
</b>
</div><br>
<%- model_class = Customer -%>

<div class="page-header">
  <!--<h5>Export to Excel | <%= link_to 'Export to Excel', url_for(:format => 'xls') %> |-->
				<!--<%= link_to "Customers", customers_path(format: "xlsx") %> |
				<%= link_to "Business", businesses_path(format: "xlsx") %> |
				<%= link_to "Co-applicants", co_applicants_path(format: "xlsx") %> |
				<%= link_to "Co-applicants Business", co_applicant_businesses_path(format: "xlsx") %>-->
<!--</h5>-->

<br><br>
</div>
<%= link_to "Add Customer", new_customer_path,:class=>"btn btn-primary btn-small", :style=>"margin-right:150px;" %>
<b style="float:right"><%= paginate @all_customer %></b>
<table class="table table-condensed">
 <thead>
  <tr>
    <th>Status </th>
    <th width="195px">Application ref no</th>
    <th width="195px">Applicant name</th>
    <th>Address</th>
    <th>Status</th>
    <th>verifing tab</th>
  </tr>
 </thead>
 <tbody>

<% @all_customer.each do |customer| %>

  <tr>
     <% if customer.application_status == 'RESIDENTIAL'%>
     <td><b style="color:#009999;border-radius:3px;padding:2px;"><%= customer.application_status %></b></td>
     <% else %>
     <td><b style="color:#CC0066;border-radius:3px;padding:2px;"><%= customer.application_status %></b></td>
     <% end %>
     <td><strong><%= link_to "scorpeo/ #{customer.created_at.to_date}/ #{customer.application_ref_no}", customer %></strong></td>
     <td><%= customer.applicant_name %></td>
     <td><%= customer.address %></td>
     <% if customer.status=='submitted' %>
     <td><span class="label label-info"> Pending </span></td>
     <% end %>
     <% if customer.status=='ready_for_verification' %>
     <td><span class="label label-info"> ready for verification </span></td>
     <% end %>

     <% if customer.status=='awaiting_to_get_verified' %>
     <td><span class="label label-warning"> in progress </span></td>
     <% end %>
     <% if customer.status=='verified' %>
     <td><span class="label label-success"> verified </span></td>
     <% end %>

     <% @verifier  = CustomerVerification.find_all_by_customer_id(customer) %>
      <% @verifier.each do |v| %>
       <% if v.tab.name %>
        <td width="150px"><%= v.tab.name %></td>
       <% else %>
        <td>no tab </td>
       <% end %>
     <% end %>
      </tr>
    <% end %>
    </tbody>
</table>


<h6  style ="float:right"><%= paginate @all_customer %></h6>
<br />
