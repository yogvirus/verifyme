require 'test_helper'

class TabTest < ActiveSupport::TestCase
  #def test_should_be_valid
test "should not save post without title" do
  post = Tab.new
  assert !tab.save, "Saved the post without a title"
end
  #end
end
